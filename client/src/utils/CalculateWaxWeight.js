const calculateWaxWeight = (mineralWeight, ayar, renk) => {
  if (mineralWeight === null) {
    return 0;
  }
  console.log("mineralWeight", mineralWeight);
  console.log("ayar", ayar);
  console.log("renk", renk);
};

export default calculateWaxWeight;
